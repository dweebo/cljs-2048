(ns com.hewittsoft.twenty48.tincan-config)

; configure
(def record-store {:endpoint "ENDPOINT_URL"
                   :username "ENDPOINT_USER"
                   :password "ENDPOINT_PWD"
                   :allowFail false })
